import 'package:flutter/material.dart';

class SentimentScore extends StatelessWidget {
  const SentimentScore({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
